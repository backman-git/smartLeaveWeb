// Generated by CoffeeScript 1.12.3
(function() {
  var People;

  People = (function() {
    function People(name, level, title, startCareerDate, availableDay) {
      this.name = name;
      this.level = level;
      this.title = title;
      this.startCareerDate = startCareerDate;
      this.availableDay = availableDay;
      this.waitHQueue = {};
      this.useDay = 0;
    }

    People.prototype.addFormToWaitHQueue = function(form) {
      return this.waitHQueue[form.fileID] = form;
    };

    People.prototype.getWaitHQueue = function() {
      return this.waitHQueue;
    };

    People.prototype.retriveFormByFID = function(FID) {
      var form;
      form = this.waitHQueue[FID];
      delete this.waitHQueue[FID];
      return form;
    };

    People.prototype.setUseDay = function(n) {
      this.useDay = n;
    };

    People.prototype.getUseDay = function(n) {
      var out;
      out = this.useDay;
      return out;
    };

    return People;

  })();

  module.exports = People;

}).call(this);
